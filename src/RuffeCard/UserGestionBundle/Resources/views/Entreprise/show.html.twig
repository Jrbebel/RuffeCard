{% extends "RuffeCardUserGestionBundle:Administrateur:Menu.html.twig" %}

{% block corp -%}
     <div class="panel panel-primary" id="clientCorps">
			                <div class="panel-heading" id="clientModifPanel">
			                    <h2 class="panel-title">
			                        <span class="fa fa-user"></span>Entreprise</h2>
			                </div>
    {% if entity is empty %}
    
    <h3>Is empty</h3>
    
    {% else %}

    <table class="record_properties table table-hover">

        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Societe ghhghg</th>
                <td>{{ entity.societe }}</td>
            </tr>
            <tr>
                <th>Numsiret</th>
                <td>{{ entity.numsiret }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ entity.taille }}</td>
            </tr>
            <tr>
                <th>Contactresp</th>
                <td>{{ entity.contactResp }}</td>
            </tr>
            <tr>
                <th>Emailresp</th>
                <td>{{ entity.emailResp }}</td>
            </tr>
            <tr>
                <th>Telresp</th>
                <td>{{ entity.telResp }}</td>
            </tr>
            <tr>
                <th>Secteuractivite</th>
                <td>{{ entity.secteuractivite }}</td>
            </tr>
        </tbody>
    </table>
 <div class="row">
     <div class="col-md-12" id="listingClientcreer">
          <a href="{{ path('Admin_entreprise_edit', { 'id': entity.id }) }}">
        	<button class="btn btn-primary">
        		<i class="fa fa-check-square-o"></i>
                Modifier
             </button>
        </a>
        <a href="{{ path('Admin_entreprise') }}">
        	<button type="submit" class ="btn btn-warning">
        		<i class="fa fa-share-square-o"></i> 
        		Retour Listing		
             </button>
        </a><a>
        
               <form action="{{ path('Admin_entreprise_delete', { 'id': entity.id }) }}" method="post">
                <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
                <button type="submit" class="btn btn-danger" ><i class="fa fa-ban"></i>	Supprimer</button>
            </form>
        
  </a>
      </div>
    </div>
    {% endif %}
</div>
{% endblock %}
